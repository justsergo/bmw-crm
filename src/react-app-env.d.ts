/// <reference types="react-scripts" />

declare module '@env' {
  const DADATA_API_KEY: string;
  const URL_PATH_ADDRESS: string;
  const NODE_ENV: 'development' | 'production' | 'test';
  const PUBLIC_URL: string;
}
